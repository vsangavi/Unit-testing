module.exports = {
  //check odd or even
  checkNumber: function (numberValue) {
    return numberValue % 2 === 0;
  },
  //check divisibility by 4
  checkDivisible: function (numberValue) {
    return numberValue % 4 === 0;
  },
};
